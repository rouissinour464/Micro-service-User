apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: gestion-projet
spec:
  selector:
    app: auth
  ports:
    - name: http
      port: 8081
      targetPort: 8081
      protocol: TCP
  type: ClusterIP
